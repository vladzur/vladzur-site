import { createStore } from 'vuex'
import releases from '@/store/modules/releases';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    releases
  }
})
